# RUPORA 文档编辑

RUPORA 在本地编辑 Markdown 文档，并提供源码、所见即所得、预览和分屏视图。

## Language

**文档（Document）**：一次打开或新建的编辑对象；另存为和从磁盘重载仍属于同一篇文档。
_Avoid_: 用标签位置或文件路径代替文档身份

**文档版本（Document revision）**：文档在某一时刻的编辑上下文。正文再次变回原样，不代表期间没有发生编辑或文件关联变化。

**文档快照（Document snapshot）**：在启动扩展或打开表格编辑器时捕获的文档版本、正文和路径。

**外部变更（External change）**：磁盘文件相对于本次编辑所依据的磁盘版本发生变化，包括修改、移动和删除。

**块（Block）**：文档中作为一个整体参与编辑或呈现的 Markdown 区域，例如段落、代码块或表格。

**渲染依赖（Rendering dependency）**：决定块最终呈现内容的其他信息，包括目录所引用的标题、引用定义和本地图片。
