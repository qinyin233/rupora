# 普通段落点击跳动与空白归属：桌面复现

本轮仅验证用户报告的段落点击后增加空白、在空白输入却接到下一段的问题，不修改生产代码。

## 初始只读观察

live 窗口枚举和 `Get-Process rupora` 起初均无 RUPORA。获主代理明确授权后启动已有 release，恢复唯一标签 `product-desktop-20260910.md`，2500 字符、303 词、162 行，无 dirty。该恢复标签没有修改。

恢复画面位于段落 56–65，焦点为第 151 行的编辑块（段落 62）。段落 61、62、63、64 的文字中心 Y 约为 470、530、633、692，62→63 约 103 像素，其余约 60 像素。编辑块下方已出现约 43 像素额外空白。

证据：`target/product-desktop-evidence/11-paragraph-gap-initial-state.json`、`12-paragraph-gap-restored.png`、`12-paragraph-gap-restored.json`。

## 专用夹具复现

新开 `target/paragraph-gap-desktop-20260910.md`，保留恢复标签。夹具为标题和三个普通段落，55 字符、7 词、8 行：

```markdown
# 段落间距测试

第一段用于点击测试。甲乙丙丁戊己庚辛。

第二段 KEEP-SECOND。

第三段 KEEP-THIRD。
```

采用固定 1243×850 窗口截图中的逻辑坐标目视测量，Y 值为文字中心近似值，不宣称逐像素自动测量。

| 操作 | 第一段 Y | 第二段 Y | 第三段 Y | 文本变化 |
| --- | --- | --- | --- | --- |
| 刚打开、未激活块 | 277 | 336 | 394 | 无 |
| 点击第一段 `(328,277)` | 275 | 379 | 437 | 无，仍55字符/8行 |

后续两段整体下移约 43 像素。第一段与第二段之间从约 59 像素增至约 104 像素，形成可点击的额外空白。

随后点击新增空白 `(236,329)`，出现空白处插入光标，可访问性焦点仍为“从第 3 行开始的所见即所得编辑块”。输入 `GAP-INPUT`，实际结果为：

```text
GAP-INPUT第二段 KEEP-SECOND。
```

输入没有形成独立段落，而是接在第二段开头。统计变为64字符、7词、8行。第二段回到约 Y=333，第三段仍约 Y=437，额外空白转移到了第二段下方，与用户描述的连续跳动一致。

证据：

- `target/product-desktop-evidence/13-gap-before-click.png`、`.json`
- `target/product-desktop-evidence/14-gap-after-first-click.png`、`.json`
- `target/product-desktop-evidence/15-gap-input-joins-next.png`、`.json`

## 预期与交接

仅点击普通段落进入编辑不应凭空增加约43像素块高；可见空白上的输入落点应与实际源码插入位置一致，不能视觉显示在独立空行、输入后却成为下一段前缀。

已 Ctrl+Z 撤销唯一一次测试输入，夹具恢复55字符、7词、8行，无dirty。原恢复标签保留且未改动。应用仍打开，活动为专用小夹具，桌面操作已停止供主代理接续；测试员未关闭/重启运行中的应用、未改生产代码。

## 修复后的 release 实机复测

已核对新构建 `target/release/rupora.exe` 文件时间 **2026-09-10 15:28:24**，大小 **35,206,144 字节**。旧版本由 GUI 正常关闭后才重建；新版启动自动恢复两个原有干净测试标签。实际新窗口 ID 为 2689632。

### 普通段落点击与段间距：通过

同一 `paragraph-gap-desktop-20260910.md`，初始第二段处于编辑状态，三段文字中心 Y 约为 **277 / 336 / 394**。按第一段 → 第三段 → 第二段 → 第一段交错点击，三段始终维持上述位置，没有再出现原先约43像素的整体下移，文本保持55字符、7词、8行且无dirty。

点击第一段与第二段之间的普通间距 `(236,309)` 后，焦点成为窗口根区域，没有在间距中出现虚假的插入光标，也没有产生额外编辑行。旧版失败点 `(236,329)` 在新布局已接近第二段文字，而非新增空白，因此本次使用普通间距中心作对应测试。

证据：`target/product-desktop-evidence/16-gap-retest-first-click.png`、`.json`；`17-gap-retest-spacing-click.png`、`.json`。

### 真实额外空行输入与撤销：通过

另开隔离夹具 `target/real-gap-desktop-20260910.md`，初始36字符、6词、7行；两段之间源码为三个换行，即 `第一段 KEEP-FIRST。\n\n\n第二段 KEEP-SECOND。`。

点击真实空行 `(234,342)` 后出现合法空行光标；输入 `GAP-NEW` 后，它成为独立中间段落，未接到下一段。正常切换源码，核对实际文本为：

```markdown
# 真实空行测试

第一段 KEEP-FIRST。

GAP-NEW

第二段 KEEP-SECOND。
```

输入后43字符、6词、8行；Ctrl+Z 一次恢复初始36字符、6词、7行，无dirty。源码可访问性文本与磁盘夹具一致，只省略磁盘末尾的一个 LF。

证据：`target/product-desktop-evidence/18-real-gap-retest-input.png`、`.json`；`19-real-gap-retest-source.json`；`20-real-gap-final-clean.json`。

### 段尾导航与最终状态

第一段 End → Right 能进入第二段（活动编辑块第3行→第5行），文档保持55字符、8行，没有修改。按主代理后续收敛要求，没有再扩展 Down 操作；它的自动回归不作为本报告实机结果。

本轮限定缺陷复测完成。测试输入已撤销，三个标签均无未保存修改；原恢复标签未改动。新版应用保持打开，活动为原 `paragraph-gap-desktop-20260910.md`，写作模式，55字符、7词、8行，供用户直接测试。桌面控制已释放。
