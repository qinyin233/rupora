# Windows 原生验收：2026-09-24

对应 [#6](https://github.com/qinyin233/rupora/issues/6)。执行者：Codex，通过
Computer Use 的原生 Windows 键盘、鼠标与窗口截图操作。此记录不是全平台验收通过。

## 环境与构建

- Windows 11 家庭中文版，x86_64，10.0.22631；系统 AppliedDPI 为 96，
  逐显示器实际缩放尚未核实。
- 实际出现中文拼音候选框；系统运行 ChsIME（微软拼音），系统组件
  `ChsIME.exe` 文件版本为 `10.0.22621.5697`。第二种输入法未测试。
- 失败构建源码：`0bf5c4389ae70487ef5d694ab0d6a3999785da9f`，
  显示版本 `2.0.0-alpha.3`，本地未签名开发包，并非已有公开 alpha.3 资产。
- NSIS SHA-256：`5690148499995ceff76ccb6627392bf93ff1f12f7b5cf3b686c22587ac99d1d1`。
- 从该包解出的实际运行程序 SHA-256：
  `967e69412b8380456216b798b9646d63fd3e585708bc9a715434f3311e678ec9`。
- 原生工具逐步截图及可访问文本保存在本次 Codex 验收任务的工具记录中；
  未另外导出独立截图文件。只编辑合成测试文档，未执行强制退出测试。

## 原生结果

模式：写作（所见即所得）。通过系统打开对话框打开 UTF-8、无末尾换行的
`A🙂B` 测试文件，未通过程序接口注入编辑器事件。

| 用例 | 实际操作与结果 | 状态 |
| --- | --- | --- |
| I1 | 点击 A 后，逐键输入 `zhongwen`，观察到拼音预编辑和“中文”候选；空格确认得到 `A中文🙂B` | 通过 |
| I4 | 对上述提交 Ctrl+Z 得到 `A🙂B`，Ctrl+Shift+Z 得到 `A中文🙂B` | 通过 |
| I3 | 光标右移过 emoji，Shift+Left 两次，可访问选区确认为 `文🙂`；逐键输入 `xin`，预编辑为 `A中xinB`；空格确认后实际为 `A中新文🙂B`，预期 `A中新B` | 失败 |

I3 的前后文本同时得到窗口截图及可访问正文确认。失败没有写回磁盘。
复测前通过撤销恢复 `A🙂B` 并正常退出旧程序。此次失败后的历史记录还出现了
包含空格的中间状态，修复构建须重新检查实际撤销，不能直接沿用 I4 的结果。

## 回归与修复

`ime_clear_before_commit_replaces_the_original_selection` 在应用级事件入口重放
Windows 的空 Preedit 紧接非空 Commit，修复前确实得到相同的
`A中新文🙂B`。winit 0.30.13 的 Windows `WM_IME_COMPOSITION` 和
`WM_IME_ENDCOMPOSITION` 路径会连续发出这两个事件。

取消处理不再把这一对事件当成独立取消，而是交给 TextEdit 处理仍保留的
预编辑缓冲区。真正取消后跟普通输入的原有回归继续保留。新测试覆盖源码及写作
编辑路径、插入及选区替换、多帧预编辑、末尾空 Preedit，以及一次撤销/重做。

## 修复构建的原生复测

源码提交：`aa4051fa19d60dffe9e90951e6d9e35ecf32d628`。Rust 1.92.0、
cargo-packager 0.11.8 构建的新 NSIS 开发包仍未签名，未发布到公开 Release。

- 包 SHA-256：`bbfeb078b4a33238e5290548eda41da901f7d8b55058d0f442c2a8633be8f41c`。
- 解包后程序 SHA-256：`7943ef4a32be4dc3b9b01e4da857bc48b9afb52bf1c11b158aa9526e4f20e0cc`，
  与构建清单中的 release 程序一致。实际从这个解包目录启动。
- 全量测试 568 项通过，4 项既有 ignored；严格 Clippy、fmt 和 release 性能门禁通过。

仍使用同一系统输入法，通过打开对话框加载新合成文件 `A中文🙂B`，初始撤销历史为空。

| 检查 | 实际操作与结果 | 状态 |
| --- | --- | --- |
| I3 复测，写作模式 | End、Left、Shift+Left 两次；选区确认为 `文🙂`；逐键 `xin`，观察候选框，空格确认得到 `A中新B` | 通过 |
| 替换的撤销/重做 | 一次 Ctrl+Z 恢复 `A中文🙂B`；一次 Ctrl+Shift+Z 恢复 `A中新B`，没有空格中间状态 | 通过 |
| 取消后继续输入（I2 补充变体） | 在 B 前输入 `x` 后 Escape，恢复 `A中新B`；再输入 `mo`，选择第三候选“末”，得到 `A中新末B` | 通过 |
| 已提交内容切换视图 | 写作切换到源码，正文仍为 `A中新末B` | 通过 |
| 保存与正常重启（P2 部分） | Ctrl+S 后磁盘恰为 `A中新末B`，UTF-8 无 BOM、无末尾换行，共 11 字节；正常退出、从同一解包位置重启，源码和写作模式均为相同正文 | 通过 |

## 多段输入与格式命令补充验收

继续使用上述修复构建、系统和解包程序。以下文字通过原生窗口的字面 Unicode
输入与键盘快捷键输入；这部分不是系统拼音组合输入测试。

| 检查 | 实际操作与结果 | 状态 |
| --- | --- | --- |
| I5 部分，写作模式 | 新建文档，输入 `甲🙂`、Return、`乙`，形成两段；从末尾 Ctrl+Shift+Home 跨段选择，输入 `新🙂段`；撤销恢复两段，重做恢复替换结果，再撤销 | 通过 |
| I7 格式与历史 | 选中第一段 `甲🙂`，依次 Ctrl+B、Ctrl+I、Ctrl+K；源码恰为 `***[甲🙂](https://)***\n\n乙`；在源码模式逐次撤销分别恢复三重星号、双星号和无格式正文，逐次重做得到对应结果，第二段完整保留 | 通过 |
| I7 两次加粗 | 重启后在写作模式选中第二段 `乙`，连续两次 Ctrl+B；第一次呈现粗体，第二次恢复普通文本；切换源码确认完整正文与保存基线相同 | 通过 |
| P2 多段与格式补充 | 通过系统另存为对话框保存为 `多段格式验收.md`，读取磁盘核对；正常退出后从同一解包程序重启，源码完整恢复，写作模式两段及粗斜体链接均保留 | 通过 |

表中 `\n` 表示实际 LF 字节。保存文件是 UTF-8 无 BOM，无尾随换行，共 30 字节：

```markdown
***[甲🙂](https://)***

乙
```

文件 SHA-256：`08ffad4fe06d530d8bf2a3f98e39ac0202372a56143f9ddd4fc712c30b85d638`。
链接命令的 `https://` 是待编辑占位地址；未测试打开链接或访问网络。
活动段落显示链接源码，离开该段后呈现链接标签；两种显示均实际观察。

提交 `4a1ad280375b7396b990e9b4ce41ac127bbf87e6` 的
[CI 35988363249](https://github.com/qinyin233/rupora/actions/runs/35988363249)
已完成：Windows、macOS、Linux 及安全/性能检查全部通过；普通 push 按配置跳过 fuzz。

以上是逐项实际结果。尚未完成 I1/I2 原始基线在修复构建上的全部模式复测、
I5 行首/行尾边界的完整矩阵、其余模式的格式操作、跨文档/焦点切换、文件关联、
强制退出恢复及其他平台。#6 保持开放；不能把这次局部通过记为整个原生验收完成。

## 源码模式新增失败：候选框位置与撤销

仍为 aa4051f 解包程序。新合成文件 `视图切换输入.md` 初始正文 `A🙂B`，
通过系统打开对话框加载，源码模式点击 A 后逐键输入 `zhongwen`。

- 拼音预编辑显示在第一行（窗口截图 y 约 225），但微软拼音候选框出现在整个
  编辑区域底部（y 约 1020）。空格确认后正文正确为 `A中文🙂B`。
- 一次 Ctrl+Z 实际恢复 `Azhong'wen🙂B`，预期 `A🙂B`；一次 Ctrl+Shift+Z
  返回 `A中文🙂B`。这是实际逐键操作，预编辑及提交之间包含工具调用与观察的停顿。
  快速事件注入的现有历史测试没有覆盖这次实际结果。该撤销缺陷仍待单独修复。
- 为保留可用测试状态，重做后的 `A中文🙂B` 已通过 Ctrl+S 保存到合成文件。

候选框回归 `editor_ime_candidate_area_tracks_the_caret_in_a_tall_editor` 经应用原生
输入适配入口执行。修复前源码模式的候选区域底部为 701.6，光标底部为 158.6，
断言失败；修复把编辑器输出的候选区域设置为现有光标矩形，保留组合中断标志。
根因是 egui-winit 0.35 将 `IMEOutput.rect` 传给系统，而 TextEdit 的该字段覆盖整个
多行编辑区域。回归覆盖源码、分栏和写作模式；实际新包复测另行记录。

同一旧构建补充 I8 关闭保护：在已保存的 `A中文🙂B` 中插入“尾”，发起 Alt+F4，
实际出现未保存确认框；点击“否”后正文仍为 `A中文尾🙂B`，继续输入“续”得到
`A中文尾续🙂B`，Ctrl+S 后磁盘与之相同。本项未包含正在组合输入时的焦点切换。

## 候选框修复的新包原生复测

- 源码提交：`d12c8efe500c6405ee208a9534cfc1993bd925fb`。
- NSIS 开发包 SHA-256：`e0d55052a15e1f99f7f5495fc2e7543bc95063e39e8e742fca714d494f1ca21e`。
- 从包内解出的实际程序 SHA-256：`77573926750f67026eedc865d2f3c914b3ab89cd418a96dabb4ce842cd16723e`，
  与独立构建的 release 程序一致。包和程序仍未签名；没有替换公开 Release。
- 全量测试 569 项通过、4 项既有 ignored；严格 Clippy、fmt 和 release 性能门禁通过。

旧程序正常退出后，从新包解出的程序启动，恢复合成文件 `A中文尾续🙂B`。
同一微软拼音输入法，在 A 后按 `z`，实际观察到候选框：

| 模式 | 实际位置与后续操作 | 状态 |
| --- | --- | --- |
| 源码 | 第一行 y 约 225，候选框 y 约 255；Escape 后恢复完整基线 | 候选定位通过 |
| 分栏 | 左侧源码第一行 y 约 225，候选框 y 约 255，横坐标跟随左栏光标 | 候选定位通过 |
| 写作 | 第一行 y 约 233，候选框 y 约 267；Escape 后恢复完整基线 | 候选定位通过 |

分栏预编辑 `z` 时直接点击“写作”，候选框消失但正文为 `Az中文尾续🙂B`，
一次撤销恢复基线。尚未确认这是系统失焦提交还是预编辑残留，因此 I6 不记为通过，
须在输入事务调查中继续核对；本次候选区域调整也没有修复先前已复现的源码撤销问题。

### 独立代码复核

范围为 `42e1187...d12c8ef`。两个只读审查分别完成，未与原生执行者共用 UI 控制。

- Standards：未发现规范违反或有依据的代码异味；IME 调整留在 EditorSurface，
  回归位于应用集成测试，符合现有模块所有权与 ADR-0001。
- Spec：未发现候选定位修复的需求遗漏、额外范围或实现错误。egui 0.35 的
  `owns_ime_events` 与 `has_focus` 一致，两个矩形都已转换为屏幕逻辑坐标，
  egui-winit 再统一缩放一次，赋值不改变组合中断标志。
- 自动回归主要断言垂直位置；水平位置、焦点转移和中断标志的专门断言尚未补齐。
  上表补充了本机三个模式的真实候选框截图观察，不代表其他平台或完整 #6 已通过。

## 输入事务与失焦预编辑修复（自动回归）

提交 `85e8038` 将源码/分栏预编辑保留在 EditorSurface 临时缓冲中，候选确认才
形成独立历史事务。回归覆盖选择候选时停顿超过打字合并窗口、取消组合、普通按键
中断、同帧输入前缀、事件队列溢出、关闭及原生焦点事件，并检查最终 IME 中断输出。

后续失焦回归经实际 `raw_input_hook` 入口复现：窗口失焦后，内部 TextEdit 焦点仍可
消费迟到的 `Preedit("ni")`，使 `A🙂B` 错变成 `Ani🙂B`。源码临时缓冲不再以原生
窗口焦点作为创建前提；写作模式丢弃失焦的未确认拼音及临时光标。测试覆盖三种编辑
模式、有无显式 WindowFocused 事件、失焦前有无组合会话共 12 种情况；正文、快照、
撤销历史保持不变，恢复焦点后继续输入和撤销均正确。

全量自动检查：582 项测试通过、4 项既有 ignored；fmt、严格全特性 Clippy、release
性能门禁通过。此处是自动回归证据，新的原生包输入法验收另行记录。

此前在 `d12c8ef` 解包程序中另做普通 Unicode 输入：新建文档，实际输入
`验收：中文🙂与 English`，撤销为空、重做恢复原文，系统另存为后磁盘内容一致，
正常退出。这验证普通文字输入，不计作逐键拼音组合验收。

## 输入事务修复后的实际窗口复测

用户要求 Computer Use 自行输入并验收后，继续执行原生窗口操作。环境仍为上述
Windows 11 x86_64、微软拼音；逐键 `press_key` 产生真实拼音候选，未注入 egui 事件。

- 源码提交：`811f8274680836c836f8574ca91c0e514e5aa197`，包含 `85e8038` 输入事务修复。
- 构建时间：2026-09-24T11:55:45Z；Rust 1.92.0，cargo-packager 0.11.8。
- NSIS 文件：`rupora_2.0.0-alpha.3_x64-setup.exe`，13,267,549 字节，SHA-256
  `4e1ba83158c2bbf7c89a6d930069f4e56a3b9371a8fb1219eb7b1796d66c12ff`。
- 从该包解出的实际运行程序：36,023,808 字节，SHA-256
  `fc0f9bb17294d8cbc1f9297eb99277c7000484dc4b1671ff448ddda2ac202078`，
  与构建记录的 release 程序相同。两者 NotSigned；未替换公开 Release。
- 通过系统打开对话框打开合成文件 `target/native-ime-transaction-fixtures/源码停顿撤销.md`，
  初始内容 `A🙂B`。截图和可访问正文均保留于本次 Computer Use 工具记录。

| 范围 | 精确操作与实际结果 | 结果 |
| --- | --- | --- |
| 源码 I1/I4 | 在 A 后逐键输入 zhongwen，每键之间观察，最终候选额外停顿 1.2 秒；空格确认得到 `A中文🙂B`。一次 Ctrl+Z 恢复 `A🙂B`，一次 Ctrl+Shift+Z 恢复 `A中文🙂B` | 通过，原生撤销缺陷不再复现 |
| 保存与模式切换 | Ctrl+S 后磁盘为 `A中文🙂B`；点击写作后显示相同 | 通过 |
| 写作 I3/I4 | 点击 emoji 后，Shift+Left 两次，可访问选区确认为 `文🙂`；逐键 xin，停顿 1.2 秒后空格确认，得到 `A中新B`；一次撤销恢复 `A中文🙂B`，一次重做恢复 `A中新B` | 通过 |
| 写作取消 | 在新后按 z，观察候选；Escape 后恢复 `A中新B` | 本次取消操作通过 |
| 写作失焦 | 再按 z，点击标题栏最小化，然后恢复窗口；候选消失，正文为 `A中新B`。重新点击正文并以字面 Unicode 输入“尾”，得到 `A中新尾B` | 本次最小化/恢复场景通过 |
| 保存与重启 | Ctrl+S，Alt+F4 正常退出，重新启动同一解包程序；恢复的写作正文与随后切换的源码均为 `A中新尾B` | 本次保存/重启通过 |

最终测试文件为 UTF-8 无 BOM、11 字节、无尾随换行，SHA-256
`bef2452645d8f53993ec9600759eb9a6e58f2bf5846055b381620033c7d420aa`。
复测结束再次正常退出程序。最小化场景不能替代所有窗口切换或迟到事件排列；新包的
分栏完整矩阵、跨文档组合、文件关联、强制退出恢复、其他输入法及其他平台仍未完成。
#6 保持开放。

该源码提交的 [CI 35995634856](https://github.com/qinyin233/rupora/actions/runs/35995634856)
已完成：Windows、macOS、Linux、安全与性能检查全部成功；普通 push 的 fuzz 按配置跳过。

## 分栏及组合输入中切换的补充验收

继续使用上节 `811f827` 的同一解包程序、Windows 和微软拼音。通过系统打开对话框
打开合成文件 `target/native-ime-transaction-fixtures/分栏停顿撤销.md`，初始为 `A🙂B`。
以下拼音均为原生逐键输入；“尾”使用字面 Unicode 输入，不计作拼音验收。

| 范围 | 操作与实际结果 | 结果 |
| --- | --- | --- |
| 分栏 I1/I4 | A 后逐键 zhongwen，最终候选额外停顿 1.2 秒；预编辑只出现在左侧，右侧仍为 `A🙂B`。空格确认后两侧均为 `A中文🙂B`；一次撤销恢复基线，一次重做恢复中文 | 通过 |
| 分栏到写作 I6 | 在 `A中文🙂B` 的文后预编辑 z，点击写作；候选及 z 消失，正文不变。一次撤销恢复 `A🙂B`，重做恢复 `A中文🙂B` | 本次模式切换通过，未增加拼音历史记录 |
| 写作切换文档 I6 | 在 A 后预编辑 z，切换到 `源码停顿撤销.md`，其正文保持 `A中新尾B`；切回分栏测试文件，正文为 `A中文🙂B`，无 z | 本次文档隔离通过 |
| 分栏 I3/I4 | Shift+Left 两次选中 `文🙂`，逐键 xin，右侧仍显示已确认正文；空格后两侧为 `A中新B`。一次撤销恢复 `A中文🙂B`，一次重做恢复 `A中新B` | 通过 |
| 分栏取消及再次组合 I2 | 撤销回 `A🙂B`，预编辑 z 后 Escape 恢复基线；逐键 mo，按候选编号 3 选择“末”，两侧得到 `A末🙂B` | 通过 |

### 组合输入期间退出：仍待分类

在未保存的 `A末🙂B` 的末后预编辑 z，左侧为 `A末z🙂B`、右侧仍为 `A末🙂B`。
Alt+F4 未出现退出确认，候选仍在；点击标题栏关闭则出现未保存确认框。
点击“否”后候选消失，两侧均为 `A末z🙂B`。一次撤销恢复 `A末🙂B`。

本次没有记录 Windows 原生 IME 事件，不能区分输入法正式提交 `z` 与应用错误保留
预编辑，也不能确定 Alt+F4 未触发退出的原因。此项不记为通过或已确认缺陷，后续需
对照原生 Commit/Preedit、焦点与关闭事件顺序；不能直接丢弃真实 Commit 来消除现象。

取消退出并撤销 z 后，重新点击末后输入“尾”，两侧均为 `A末尾🙂B`。Ctrl+S 后
磁盘内容完全相同，UTF-8 无 BOM、12 字节、无尾随换行，SHA-256 为
`71ab10af8b453d2ff4124a40ddd9c6427e9e5b23e4b3a6254339f20be817f669`。
保存后 Alt+F4 正常退出，窗口枚举确认进程窗口已关闭。

本轮未修改产品源码；文档提交 `bb7c8ce` 的
[CI 35996810230](https://github.com/qinyin233/rupora/actions/runs/35996810230)
全部必需作业成功，普通 push 的 fuzz 按配置跳过。#6 保持开放，组合退出的事件分类、
文件关联、强制退出恢复、其他输入法及跨平台原生矩阵仍需完成。

## 关闭确认时拼音提交的事件诊断

为分类上节的现象，在 `4579f14` 上临时给 `raw_input_hook` 增加只读记录，位置在
应用输入调度之前，只对合成文件 `分栏停顿撤销.md` 生效。记录事件类型、字符数量、
焦点和关闭状态，不记录正文或输入文本。此诊断程序由 Rust 1.92.0 release 构建，
SHA-256 为 `e8804ca73c567ee83f95963f6984589dbf7ec3b3fb1274fe8c6611d12f69ebf2`；
它不是上节的 NSIS 验收程序，也不作为新发布资产。

分栏模式恢复 `A末尾🙂B`，在尾后 Backspace 得到未保存的 `A末🙂B`。在末后按 z，
候选出现，左侧显示拼音，右侧保持已确认正文。执行 Alt+F4、点击标题栏关闭、在确认框
选择“否”，结果仍为 `A末z🙂B`。入口记录如下，括号内数字为文本字符数：

```text
time=41.280433  focused=true close=false body_chars=4 events=[Preedit(1)]
time=52.776715  focused=true close=false body_chars=4 events=[F4(pressed=false,alt=true)]
time=61.663224  focused=true close=true  body_chars=4 events=[]
time=85.170871  focused=true close=false body_chars=4 events=[Focus(false), Preedit(0), Commit(1), Focus(true)]
```

一次撤销恢复 `A末🙂B`。再次按 z，直接点击关闭（省略 Alt+F4），选择“否”，结果相同：

```text
time=106.969479 focused=true close=false body_chars=4 events=[Preedit(1)]
time=114.255630 focused=true close=true  body_chars=4 events=[]
time=133.013421 focused=true close=false body_chars=4 events=[Focus(false), Preedit(0), Commit(1), Focus(true)]
```

两次都在正文仍为 4 字符时收到明确的 1 字符 Commit，随后两侧均为 `A末z🙂B`。
因此，这两次是原生输入后端交付正式提交，应用应保留且允许撤销；不属于无 Commit
而错误写回预编辑的情形。Alt+F4 那次入口只收到 F4 松开，没有关闭请求，故不把它
归因于应用丢弃 Close；更底层的按键消费位置没有记录，仍不能由此确定。

新增 `ime_commit_after_modal_focus_round_trip_is_one_undoable_edit` 重放所记录的
焦点往返、空 Preedit、Commit 顺序，验证提交结果、单次撤销/重做边界，以及继续输入。
三模式重放均通过；实际窗口诊断只执行分栏，测试不模拟系统确认框，也不冒充三模式原生验收。

诊断结束通过两次撤销回到磁盘基线 `A末尾🙂B`，正常退出。临时探针已从源码移除，
本轮保留的是测试与证据，没有改变产品输入逻辑。原始日志、探针 diff 和诊断程序保留在
忽略目录 `target/ime-close-diagnostic/`。#6 仍开放，其他原生矩阵和恢复验收继续执行。

验证：583 项测试通过、4 项既有 ignored；fmt、严格全特性 Clippy、release 性能门禁
通过。`4579f14` 的 [CI 35998537904](https://github.com/qinyin233/rupora/actions/runs/35998537904)
全部必需作业成功，普通 push 的 fuzz 按配置跳过。

### Standards 复核

独立只读复核未发现规范违反或需要处理的代码 smell。新增测试沿现有应用入口与模块
边界执行；记录区分临时程序、真实分栏验收和三模式重放。

### Spec 复核

独立只读复核未发现本轮需求遗漏、范围扩大或错误实现；正式 Commit 的分类有两次
入口记录支持。两路共同建议把“正文仅改变一次”收紧为提交结果和单次历史边界，已采纳。

## 写作模式行边界与跨段真实组合输入

继续使用 `811f827` 的 NSIS 解包程序，运行前重新核对程序 SHA-256 为
`fc0f9bb17294d8cbc1f9297eb99277c7000484dc4b1671ff448ddda2ac202078`。
系统和输入法仍为上述 Windows 11 x86_64、微软拼音；本节全部拼音通过真实逐键操作，
不是字面 Unicode 注入。通过系统打开对话框打开新合成文件
`target/native-ime-transaction-fixtures/行首行尾输入.md`，初始源码为 `甲🙂\n\n乙`，
UTF-8 无 BOM，SHA-256 为
`fcaf6506ac72de04300ad61cda8b4a946a444a27a9f365b9100bb036815b996e`。
下表 `\n` 表示实际 LF。

| 范围 | 精确操作及结果 | 状态 |
| --- | --- | --- |
| I5 行首 | 点击第一段，Home，逐键 ni，观察“你”候选并空格确认；得到 `你甲🙂\n\n乙` | 通过 |
| I5 行尾 | End 到第一段 emoji 后，逐键 mo，观察第一候选“末”并空格确认；得到 `你甲🙂末\n\n乙` | 通过 |
| 两次提交的历史边界 | 两次 Ctrl+Z 依次恢复 `你甲🙂\n\n乙`、`甲🙂\n\n乙`；两次 Ctrl+Shift+Z 依次恢复两次提交，emoji 和第二段完整 | 通过 |
| I5 跨段替换 | 点击第二段并 End，Ctrl+Shift+Home；截图确认两段均选中，可访问 selected_text 仅报告第一块。逐键 xin，空格确认后正文仅为 `新`，一次撤销恢复两段，一次重做恢复 `新` | 通过 |
| 跨段组合取消 | 撤销回两段及跨段选区，按 z 观察候选，再 Escape；恢复 `你甲🙂末\n\n乙`，无拼音残留 | 本次取消通过 |
| 源码与保存 | 点击源码，可访问正文确认为 `你甲🙂末\n\n乙`；Ctrl+S 后磁盘完全相同 | 通过 |

跨段预编辑期间第一块显示 xin、第二块仍高亮；确认后原跨段选区被完整替换。
这一临时显示与最终已提交正文分别记录，不把单块可访问选区当作整篇选择的证据。
最终文件 UTF-8 无 BOM、18 字节、无尾随换行，SHA-256 为
`323ba772de7b1143a6f1a5e10f4abbcaa587822e7d9084cc732140a4e245c148`。
保存后 Alt+F4 正常退出，从同一解包路径重启，源码完整恢复上述两段及空行；点击写作，
截图确认两段中文与 emoji 排版一致。重启后的写作可访问 document_text 只报告第一块，
未用其证明整篇正文；完整源码和窗口截图分别核对。复测完成后再次正常退出。

本轮没有修改产品代码；`e4d856c` 的
[CI 35999763706](https://github.com/qinyin233/rupora/actions/runs/35999763706)
全部必需作业成功，普通 push 的 fuzz 按配置跳过。本节补充写作模式 I5，不代表源码、
分栏的行边界矩阵、完整 #6 或其他平台原生验收已完成。
