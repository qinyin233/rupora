# RUPORA 界面约定

## 产品与方向

面向日常使用中文、英文和代码片段的桌面 Markdown 作者。用户要求完整重塑前端，采用精致、克制的 Apple 风格；所有图标必须简约且统一。保留 Windows 原生窗口和 Ctrl 快捷键，不把桌面编辑器做成营销页。

本轮采用“文稿书桌”：一条稳定的工具轨组织应用操作，文稿架承载导航，连续纸面承载写作。活动文件身份与编辑方式放在纸面上方，不再把品牌、文件操作和视图开关堆进同一排按钮。

```text
┌────┬────────────────┬────────────────────────────────────┐
│ R  │ 文稿           │ 活动文件名称          写作 / 视图 │
│    │                │ 保存状态              保存 · 更多 │
│ 文 │ 新建文稿       ├────────────────────────────────────┤
│ 稿 │ 打开的文稿     │ 轻量文稿标签（多文稿时）             │
│ 查 │  活动文件      ├─────────────────────────────┬──────┤
│ 找 │  其他文件      │                             │ 大纲 │
│ 打 │                │  连续、左对齐的正文        │      │
│ 开 │ 文件夹         │                             │      │
│    │ 最近打开       │  随可用宽度调整留白        │      │
│ 外 │                │                             │      │
│ 观 │                ├─────────────────────────────┴──────┤
└────┴────────────────┴──状态──────────────────────统计───┘
```

备选的“保留全宽工具栏，再换配色与圆角”不采用：它无法回应用户明确指出的结构缺乏变化。也不采用悬浮工具面板和多层圆角卡片，避免遮挡正文和增加视觉噪声。文稿架关闭后工具轨仍在，重新打开、查找、新建和外观操作始终可达。

## 视觉标记

浅色核心：纸白 `#FFFFFF`、雾白 `#F3F4F6`、铝灰 `#ECEEF2`、墨色 `#20242C`、辅助文字 `#596170`、操作蓝 `#0067D9`。深色使用独立语义色而非直接反转。正文区域纯净、无投影；只在当前文稿、键盘焦点和明确操作上使用蓝色。

字体保留本地中文字体与等宽代码字体。活动文稿名称 17 pt、导航标题 20 pt、界面标签 13 pt、辅助信息 12 pt；正文仍为 17 pt / 27 pt，避免改变输入映射和块边界。文字全部左对齐，长文稿名省略且保留完整提示。标签使用细活动线而不是立体胶囊。

图标统一在 20×20 网格上绘制，主要线宽 1.5 pt，线端圆润、光学居中、轮廓表达。常规按钮 32 pt，工具轨按钮 40 pt，关闭按钮 24 pt。图形尺寸随承载区域等比缩放；不得使用 emoji、Unicode 箭头或来自字体的杂乱图标。图标旁已有文字时作为装饰；独立按钮有准确名称和悬停说明，视图和导航按钮暴露选中状态。

## 工作流与空间优先级

- 工具轨：文稿导航、查找、新建、打开、外观。全局菜单保留全部既有操作。
- 文稿架：标题与数量、新建操作、可关闭的已打开文稿、工作区目录、最近文件；不放空工作区说明卡片。
- 上下文工具栏：活动文稿名称与路径/未保存状态、保存、视图分段开关、更多。宽度不足时视图只显示轮廓图标，完整名称保留在提示中。
- 多文稿标签：固定边界、活动下划线、未保存圆点和一直可达的关闭按钮；超长文稿横向滚动并自动显露活动项。
- 大纲：轻量标题列表、清楚的层级、空状态说明；占用正文侧边，不挤压全局工具栏。
- 欢迎：左对齐“留一页给想法”，明确新建/打开操作与键盘入口；空文稿仍直接进入既有输入流程，不用覆盖层截获光标。
- 最小 820×560 窗口优先保留工具轨、正文和主要操作，导航宽度有上限；分栏先压缩正文留白。查找表单按可用宽度布局，不能被导航挤出窗口。

## 行为与验收

颜色、图标、标签和页面度量集中于 `native/src/presentation.rs`。写作、源码和阅读共用页面度量；选中、悬停、按下、键盘焦点只改变颜色与描边，不改变尺寸。普通文字及辅助文字在各背景上至少 4.5:1 对比度，功能图标至少 3:1。

不加入装饰动画、远程字体、WebView 或新的框架。不改变换行、撤销、代码块和保存语义。验收必须覆盖明暗主题、820×560、长文件名、多文稿关闭、查找替换、视图切换、双侧栏与分栏并存，以及真实窗口的输入和点击。自动化通过不能代替窗口观察。
